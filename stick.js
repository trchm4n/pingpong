(()=>{const stick=document.querySelector('#moveStick');if(!stick)return;const knob=stick.querySelector('.stickKnob');window.pingPongStickX=0;let pointerId=null;const move=e=>{if(e.pointerId!==pointerId)return;const r=stick.getBoundingClientRect(),limit=r.width*.29;let dx=e.clientX-r.left-r.width/2,dy=e.clientY-r.top-r.height/2,d=Math.hypot(dx,dy);if(d>limit){dx*=limit/d;dy*=limit/d}knob.style.transform=`translate(calc(-50% + ${dx}px), calc(-50% + ${dy}px))`;window.pingPongStickX=dx/limit};const release=e=>{if(e.pointerId!==pointerId)return;pointerId=null;window.pingPongStickX=0;knob.style.transform='translate(-50%,-50%)'};stick.addEventListener('pointerdown',e=>{e.preventDefault();pointerId=e.pointerId;stick.setPointerCapture(pointerId);move(e)});stick.addEventListener('pointermove',move);stick.addEventListener('pointerup',release);stick.addEventListener('pointercancel',release)})();